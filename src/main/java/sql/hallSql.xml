<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hall">
	<select id="hallInfoSQL" parameterType="int" resultType="hall.model.HallDTO">
		select * from hall where idx=#{idx}
	</select>
	<select id="roomInfoSQL" parameterType="int" resultType="room.model.RoomDTO">
		select * from room where hallIdx=#{idx}
	</select>
	<select id="hallPhotoSQL" parameterType="int" resultType="hallPhoto.model.HallPhotoDTO">
		select * from hallPhoto where hallIdx=#{idx}
	</select>
</mapper>
