<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hall">
	<!-- hall -->
	<select id="hallInfoSQL" parameterType="int" resultType="hall.model.HallDTO">
		select * from hall where idx = #{idx}
	</select>
	<select id="hallCompareFormSQL" resultType="hall.model.HallDTO">
		select * from hall
	</select>
	<select id="hallAddSearchByGuSQL" parameterType="String" resultType="hall.model.HallDTO">
		select * from hall where gu = #{gu}
	</select>
	<select id="hallAddSearchByNameSQL" parameterType="String" resultType="hall.model.HallDTO">
		select * from hall where name = #{name}
	</select>
	<insert id="inserthall" parameterType="hall.model.HallDTO">
		insert into 
	hall
	values(hall_idx.nextval,
		#{name},
		0.0,
		0,
		#{si},
		#{gu},
		#{dong},
		#{addr},
		#{roadAddr},
		#{hallType},
		#{mealCost},
		#{menuType},
		#{guest},
		#{ckPoint},
		#{locationX},
		#{locationY},
		#{traffic},
		#{line},
		#{station})
	</insert>
	<update id="updatehall" parameterType="hall.model.HallDTO">
		update hall
		set
		name = #{name},
		grade = #{grade},
		accrue = #{accrue},
		si = #{si},
		gu = #{gu},
		dong = #{dong},
		addr = #{addr},
		roadAddr = #{roadAddr},
		hallType = #{hallType},
		mealCost = #{mealCost},
		menuType = #{menuType},
		guest = #{guest},
		ckPoint = #{ckPoint},
		locationX = #{locationX},
		locationY = #{locationY},
		traffic = #{traffic},
		line = #{line},
		station = #{station}
		where idx=#{idx}
	</update>
	<!-- room -->
	<select id="roomInfoSQL" parameterType="int" resultType="room.model.RoomDTO">
		select * from room where hallIdx = #{hallIdx}
	</select>
</mapper>