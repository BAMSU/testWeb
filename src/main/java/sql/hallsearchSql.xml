<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hallsearch">
	
	<select id="hallsearchSQL" resultType="hall.model.HallDTO">
		select * from hall order by idx desc

	</select>


	<select id="hall_searchview1" parameterType="map" resultType="hall.model.HallDTO">
		select * from hall where hallType like '%'||#{hallType}||'%' and menuType
		like '%#{menuType}%'
		<!-- <if test="hallType!=null"> and hallType like '%#{hallType}%' </if> -->
		<!-- <if test="menuType!=null"> and menuType like '%#{menuType}%' </if> -->
		<if test="line!=null">
			and line = #{line}
		</if>
		<if test="name!=null">
			and name = #{name}
		</if>
	</select>

	<select id="hall_searchview2" resultType="hall.model.HallDTO">
		select * from hall_estimate where est_guest like '%#{est_guest}%' and
		meal_price1 like '%#{meal_price1}'
		and meal_price2 like '%#{meal_price2}'

	</select>

	<select id="hall_estlistSQL" parameterType="map" resultType="hallEstimate.model">
		select * from hall_estimate order by idx desc
	</select>

	<select id="hallranking" resultType="hallEstimate.model">
		select * from hall order by accrue desc
	</select>

	<select id="hallemt" resultType="hallEstimate.model">
		select hall.idx,hall_estimate.hall_idx from hall,hall_estimate
		where hall.idx = hall_estimate.hall_idx
	</select>

	<select id="hall3" resultType="hallEstimate.model"	parameterType="int">
		select * from hall_estimate where hall_idx = #{idx}
	</select>
	<select id="hall4" resultType="hall.model.HallDTO"	parameterType="int">
		select * from hall where idx = #{idx}
	</select>

	<select id="hallroom" resultType="room.model.RoomDTO"	parameterType="int">
		select * from room where hallIdx=#{idx}
	</select>


</mapper>